# Plugin template

Implement `name`, `version`, `capabilities`, `run`, `parse` and `validate` through the shared plugin contract, then publish the plugin object through the `lfsrc_harness.plugins` entry-point group.
