# 安全策略

## 适用范围与边界

本仓库包含 LfSrcHarness 自有源码、安装脚本和插件接口。安全重点是范围限制、
审批与急停、API 访问控制、密钥存储、外部工具调用、证据与审计日志完整性，
以及安装流程的下载来源和签名验证。第三方扫描工具与本机 Agent 副本不在本仓库内。

## 报告安全问题

请优先使用本仓库 GitHub 的私密漏洞报告入口（若已启用）。不要在公开 Issue
中发布可利用细节、密钥、真实目标或客户数据。如果暂未提供私密入口，请先提交
不含敏感细节的 Issue 请求私下联系，再提供最小化复现信息。

一般错误、文档问题和功能建议可以使用公开 Issue。报告应说明受影响版本、
预期/实际行为、复现条件和影响，不要对未经授权的系统进行验证。

## 安全不变量

- 未进入明确授权范围的目标或动作应被拒绝。
- 高风险和特权执行必须经过策略审批，不得通过插件绕过。
- API 密钥不得出现在响应、日志、仓库或报告中。
- 审计事件和证据哈希应可核验，急停应阻止后续执行。
- 安装器获取外部组件时应使用官方来源并校验其真实性。

这些是设计目标，不是对所有部署环境的安全保证。发现违反上述边界的可达问题，
尤其是未经授权执行、密钥泄露或安装供应链问题，请按私密方式报告。
